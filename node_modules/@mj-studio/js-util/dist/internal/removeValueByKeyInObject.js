import is from './is';
export function removeValueByKeyInObject(v, key) {
    if (!is.object(v)) {
        return v;
    }
    const ret = {};
    Object.entries(v).forEach(([k, value]) => {
        if (is.array(key)) {
            if (!key.includes(k)) {
                ret[k] = value;
            }
        }
        else {
            if (k !== key) {
                ret[k] = value;
            }
        }
    });
    return ret;
}
//# sourceMappingURL=removeValueByKeyInObject.js.map