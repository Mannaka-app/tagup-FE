export default function withMinimumResolveTime(minimumMilli, promise) {
    return new Promise((resolve, reject) => {
        const start = Date.now();
        promise
            .then((result) => {
            const diff = Date.now() - start;
            if (diff > minimumMilli) {
                resolve(result);
            }
            else {
                setTimeout(() => {
                    resolve(result);
                }, minimumMilli - diff);
            }
        })
            .catch(reject);
    });
}
//# sourceMappingURL=withMinimumResolveTime.js.map