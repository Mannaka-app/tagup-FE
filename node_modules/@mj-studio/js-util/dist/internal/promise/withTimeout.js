export default function withTimeout(milli, promise) {
    return Promise.race([
        promise,
        new Promise((resolve, reject) => setTimeout(() => {
            reject(new Error('Promise timeout in withTimeout<T>'));
        }, milli)),
    ]);
}
//# sourceMappingURL=withTimeout.js.map