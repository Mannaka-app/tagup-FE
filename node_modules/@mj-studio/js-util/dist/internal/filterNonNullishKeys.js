const defaultOptions = {
    preserveNull: false,
    preserveUndefined: false,
    excludeEmptyString: false,
};
export function filterNonNullishKeys(source, options = defaultOptions) {
    const mergedOptions = Object.assign(Object.assign({}, defaultOptions), options);
    const ret = {};
    Object.entries(source).forEach(([k, v]) => {
        if (v === null && !mergedOptions.preserveNull) {
            return;
        }
        if (v === undefined && !mergedOptions.preserveUndefined) {
            return;
        }
        if (typeof v === 'string' && !v && mergedOptions.excludeEmptyString) {
            return;
        }
        ret[k] = v;
    });
    return ret;
}
//# sourceMappingURL=filterNonNullishKeys.js.map