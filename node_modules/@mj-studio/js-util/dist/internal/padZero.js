import is from './is';
export function padZero(number, len = 2) {
    if (!is.number(number)) {
        return '';
    }
    return (number + '').padStart(len, '0');
}
//# sourceMappingURL=padZero.js.map