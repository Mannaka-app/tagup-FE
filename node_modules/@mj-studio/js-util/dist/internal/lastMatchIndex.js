export function lastMatchIndex(str, match) {
    for (let i = str.length - match.length; i >= 0; i--) {
        if (str.substring(i, i + match.length) === match) {
            return i;
        }
    }
    return -1;
}
//# sourceMappingURL=lastMatchIndex.js.map