export default function groupByObject(collection, getKey) {
    return collection.reduce((acc, cur) => {
        const key = typeof getKey === 'function' ? getKey(cur) : getKey;
        (acc[key] = acc[key] || []).push(cur);
        return acc;
    }, {});
}
//# sourceMappingURL=groupByObject.js.map